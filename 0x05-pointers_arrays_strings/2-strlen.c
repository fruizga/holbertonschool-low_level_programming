#include "holberton.h"
/**
 *_strlen - Returns Array length
 *Description: Returns length of an array
 *@s: array
 *@len: variable to iterate
 */
int _strlen(char *s)
{
int len;
for (len = 0; s[len]; len++)
{
}
return (len);
}

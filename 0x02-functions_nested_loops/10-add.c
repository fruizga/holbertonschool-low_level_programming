#include "holberton.h"
/**
 * add - function add two integers
 * Return: value of addition
 * @a: holds value of first integer
 * @b: holds value of second integer
 */
int add(int a, int b)
{
int sum;

sum = (a + b);
return (sum);
}
